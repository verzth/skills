# HTML Template for em-thinking Artifacts

Self-contained HTML template untuk render artifact em-thinking jadi human-readable review version.

---

## Used By

- **`/em-plan`** → renders `edd.html` (companion ke `edd.md`)
- **`/em-review` Mode A** → renders `pr-review-<sha>.html` (companion ke `pr-review-<sha>.md`)

Skills lain (`/em-works`, `/em-review` Mode B) saat ini emit `.md` only. Future v1.5+ kemungkinan extend coverage.

---

## Why Dual Output (.md + .html)

| File | Audience | Purpose |
|------|----------|---------|
| `edd.md` | Engineer (handoff), version control, future skill consumption | Source-of-truth, editable, diff-friendly |
| `edd.html` | EM, stakeholders, human reviewer | Self-contained, styled, print-friendly, share-able |

**Both wajib di-write per skill invocation.** Konten harus konsisten 1:1 (same data, beda rendering).

---

## Full Template Skeleton

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ARTIFACT_TITLE}}</title>
  <style>
    /* See "Inline CSS" section below — copy entire CSS block */
  </style>
</head>
<body>
  <div class="artifact-wrapper">
    <header class="artifact-header">
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="#">em-thinking</a>
        <span class="separator">›</span>
        <a href="#">{{SKILL_NAME}}</a>
        <span class="separator">›</span>
        <span aria-current="page">{{ARTIFACT_SHORT_TITLE}}</span>
      </nav>
      <h1>{{ARTIFACT_TITLE}}</h1>
      <div class="meta">
        <span class="badge risk-{{RISK_TIER_LOWER}}">{{RISK_TIER_LABEL}}</span>
        <span>📅 {{DATE}}</span>
        <span>🤖 by {{GENERATED_BY}}</span>
        <span>🔗 Source: <code>{{SOURCE_REFERENCE}}</code></span>
      </div>
    </header>

    <nav class="toc" aria-label="Table of contents">
      <h2>Contents</h2>
      <ol>
        {{TOC_ITEMS_FROM_H2_H3}}
      </ol>
    </nav>

    <main class="artifact-content">
      {{RENDERED_SECTIONS}}
    </main>

    <footer>
      <p>Generated by <strong>em-thinking</strong> · @verzth/skills</p>
      <p>Source markdown: <code>{{ARTIFACT_FILENAME_MD}}</code></p>
    </footer>
  </div>
</body>
</html>
```

---

## Inline CSS (copy verbatim)

```css
*, *::before, *::after { box-sizing: border-box; }

html {
  font-size: 16px;
  scroll-behavior: smooth;
}

body {
  margin: 0;
  font-family: system-ui, -apple-system, "Segoe UI", "Helvetica Neue", sans-serif;
  font-size: 1rem;
  line-height: 1.6;
  color: #1f2937;
  background: #f9fafb;
}

.artifact-wrapper {
  max-width: 920px;
  margin: 0 auto;
  padding: 32px 24px;
}

/* Typography */
h1 { font-size: 1.875rem; font-weight: 700; margin: 0 0 0.5em; color: #111827; }
h2 {
  font-size: 1.5rem; font-weight: 600;
  margin: 2em 0 0.75em; padding-bottom: 0.3em;
  border-bottom: 2px solid #e5e7eb; color: #111827;
}
h3 { font-size: 1.25rem; font-weight: 600; margin: 1.5em 0 0.5em; color: #1f2937; }
h4 { font-size: 1.0625rem; font-weight: 600; margin: 1.25em 0 0.5em; color: #374151; }
p { margin: 0.75em 0; }

a { color: #2563eb; text-decoration: none; }
a:hover { text-decoration: underline; }
strong { font-weight: 600; color: #111827; }

code {
  font-family: "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;
  font-size: 0.9em;
  padding: 0.15em 0.4em;
  background: #f3f4f6;
  border-radius: 3px;
  color: #be123c;
}

pre {
  background: #f3f4f6;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  padding: 16px;
  overflow-x: auto;
  font-family: "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;
  font-size: 0.875rem;
  line-height: 1.5;
}

pre code { background: none; padding: 0; color: inherit; }

/* ASCII diagram */
pre.ascii-diagram {
  background: #fafafa;
  border-color: #d1d5db;
  font-size: 0.875rem;
  line-height: 1.45;
  letter-spacing: 0.02em;
  color: #1f2937;
}

/* Header */
.artifact-header {
  border-bottom: 1px solid #e5e7eb;
  padding-bottom: 1.25em;
  margin-bottom: 1.5em;
}

.artifact-header .meta {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5em 1em;
  margin-top: 0.75em;
  font-size: 0.875rem;
  color: #6b7280;
}

.artifact-header .meta code {
  font-size: 0.85em;
}

/* Breadcrumb */
.breadcrumb {
  font-size: 0.875rem;
  color: #6b7280;
  margin-bottom: 0.75em;
}

.breadcrumb a { color: #6b7280; }
.breadcrumb a:hover { color: #2563eb; }
.breadcrumb .separator { margin: 0 0.5em; color: #d1d5db; }
.breadcrumb [aria-current="page"] { color: #1f2937; font-weight: 500; }

/* TOC */
nav.toc {
  background: #ffffff;
  border: 1px solid #e5e7eb;
  border-radius: 6px;
  padding: 1em 1.25em;
  margin: 0 0 2em;
}

nav.toc h2 {
  margin: 0 0 0.5em;
  border: none;
  padding: 0;
  font-size: 0.8125rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: #6b7280;
  font-weight: 600;
}

nav.toc ol { margin: 0; padding-left: 1.25em; }
nav.toc ol ol { padding-left: 1em; margin-top: 0.25em; }
nav.toc li { margin: 0.2em 0; font-size: 0.9375rem; }
nav.toc a { color: #374151; }
nav.toc a:hover { color: #2563eb; }

/* Tables */
table {
  width: 100%;
  border-collapse: collapse;
  margin: 1em 0;
  font-size: 0.9375rem;
}

table thead th {
  background: #f3f4f6;
  text-align: left;
  font-weight: 600;
  padding: 0.6em 0.8em;
  border-bottom: 2px solid #d1d5db;
  color: #1f2937;
}

table tbody td {
  padding: 0.5em 0.8em;
  border-bottom: 1px solid #e5e7eb;
  vertical-align: top;
}

table tbody tr:nth-child(even) { background: #f9fafb; }

/* Failure modes table — critical gap highlight */
table tbody tr.critical-gap { background: #fef2f2; }
table tbody tr.critical-gap td { border-bottom-color: #fecaca; }
table tbody tr.critical-gap td:first-child {
  border-left: 3px solid #dc2626;
  padding-left: calc(0.8em - 3px);
  font-weight: 600;
}

/* Badges */
.badge {
  display: inline-block;
  padding: 0.15em 0.6em;
  font-size: 0.8125rem;
  font-weight: 600;
  border-radius: 3px;
  color: #ffffff;
  letter-spacing: 0.02em;
  white-space: nowrap;
}

/* Risk tier */
.badge.risk-t0 { background: #dc2626; }
.badge.risk-t1 { background: #ea580c; }
.badge.risk-t2 { background: #2563eb; }
.badge.risk-t3 { background: #6b7280; }

/* Severity */
.badge.severity-block { background: #dc2626; }
.badge.severity-major { background: #ea580c; }
.badge.severity-minor { background: #ca8a04; }
.badge.severity-info  { background: #6b7280; }

/* Forcing question callout */
.forcing-question {
  background: #eff6ff;
  border-left: 3px solid #2563eb;
  padding: 0.75em 1em;
  margin: 1em 0;
  border-radius: 0 4px 4px 0;
  font-size: 0.9375rem;
}

.forcing-question::before {
  content: "💭 ";
  font-weight: 600;
}

/* Anti-pattern callout */
.anti-pattern {
  background: #fef2f2;
  border-left: 3px solid #dc2626;
  padding: 0.5em 0.75em;
  margin: 0.5em 0;
  border-radius: 0 4px 4px 0;
  font-size: 0.9375rem;
}

.anti-pattern::before {
  content: "❌ ";
  font-weight: 600;
}

/* Component diagram (HTML/CSS — alternative ke ASCII untuk component boundary) */
.component-diagram {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.5em;
  padding: 16px;
  background: #fafafa;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  margin: 1em 0;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 0.875rem;
}

.component-diagram .row {
  display: flex;
  align-items: center;
  gap: 0.5em;
  flex-wrap: wrap;
}

.component {
  padding: 0.5em 0.875em;
  background: #ffffff;
  border: 1px solid #d1d5db;
  border-radius: 4px;
  font-weight: 500;
  white-space: nowrap;
}

.component[data-type="service"]  { background: #dbeafe; border-color: #93c5fd; }
.component[data-type="external"] { background: #fef3c7; border-color: #fcd34d; }
.component[data-type="database"] { background: #dcfce7; border-color: #86efac; }
.component[data-type="queue"]    { background: #ede9fe; border-color: #c4b5fd; }
.component[data-type="cache"]    { background: #fce7f3; border-color: #f9a8d4; }

.arrow {
  color: #6b7280;
  font-size: 1.1rem;
  user-select: none;
}

.arrow-label {
  font-size: 0.75rem;
  color: #6b7280;
  font-style: italic;
}

/* TL;DR card */
.tldr-card {
  background: #ffffff;
  border: 1px solid #d1d5db;
  border-left: 3px solid #2563eb;
  border-radius: 6px;
  padding: 1em 1.25em;
  margin: 1em 0 1.5em;
}

.tldr-card h2 { margin: 0 0 0.5em; border: none; padding: 0; font-size: 1.0625rem; }
.tldr-card p { margin: 0.4em 0; }

/* Footer */
footer {
  margin-top: 4em;
  padding-top: 1.5em;
  border-top: 1px solid #e5e7eb;
  font-size: 0.875rem;
  color: #6b7280;
  text-align: center;
}

footer p { margin: 0.25em 0; }
footer code { background: transparent; color: inherit; padding: 0; }

/* Print */
@media print {
  body { background: #ffffff; color: #000000; }
  .artifact-wrapper { padding: 0; max-width: none; }
  nav.toc { display: none; }
  .breadcrumb { display: none; }
  h2 { page-break-before: auto; page-break-after: avoid; }
  table, pre, .component-diagram { page-break-inside: avoid; }
  .badge {
    border: 1px solid currentColor;
    background: transparent !important;
    color: inherit !important;
  }
  .forcing-question, .anti-pattern, .tldr-card {
    background: transparent;
    border-left-color: #999999;
  }
  a { color: inherit; text-decoration: underline; }
  footer { color: #666666; }
}

/* Mobile */
@media (max-width: 640px) {
  .artifact-wrapper { padding: 16px 12px; }
  h1 { font-size: 1.5rem; }
  h2 { font-size: 1.25rem; }
  table { font-size: 0.875rem; }
  table thead th, table tbody td { padding: 0.4em 0.5em; }
  .artifact-header .meta { flex-direction: column; gap: 0.25em; }
  pre, pre.ascii-diagram { font-size: 0.8125rem; }
  .component-diagram { padding: 12px; }
}
```

---

## Section Rendering Conventions

### Risk tier badge inline

```html
<span class="badge risk-t0">T0 — Critical</span>
<span class="badge risk-t1">T1 — High</span>
<span class="badge risk-t2">T2 — Standard</span>
<span class="badge risk-t3">T3 — Trivial</span>
```

Pakai di header meta + di tempat lain di body kalau perlu inline mention.

### Severity badge

```html
<span class="badge severity-block">Block</span>
<span class="badge severity-major">Major</span>
<span class="badge severity-minor">Minor</span>
<span class="badge severity-info">Info</span>
```

### TL;DR card (top of artifact)

```html
<div class="tldr-card">
  <h2>TL;DR</h2>
  <p><strong>Problem:</strong> ...</p>
  <p><strong>Architecture summary:</strong> ...</p>
  <p><strong>Critical invariants:</strong> ...</p>
  <p><strong>Critical gaps:</strong> 0 (must resolve before em-works)</p>
  <p><strong>Recommended path:</strong> proceed to /em-works</p>
</div>
```

### ASCII diagram (default for all diagram types)

```html
<pre class="ascii-diagram">
┌─────────────────────┐
│   PaymentService    │
│  ┌───────────────┐  │
│  │  Idempotency  │  │
│  │     Cache     │  │
│  └───────────────┘  │
└──────────┬──────────┘
           │ calls
           ▼
┌─────────────────────┐
│ PaymentProviderAPI  │
└─────────────────────┘
</pre>
```

### Component diagram (HTML/CSS — optional alternative untuk component boundary)

Pakai kalau component diagram-nya simple linear flow dan benefit dari color coding. Untuk diagram complex (multi-arrow, branching), tetep ASCII.

```html
<div class="component-diagram">
  <div class="row">
    <div class="component" data-type="service">PaymentService</div>
    <span class="arrow">→</span>
    <span class="arrow-label">calls</span>
    <div class="component" data-type="external">PaymentProviderAPI</div>
  </div>
  <div class="row">
    <div class="component" data-type="service">PaymentService</div>
    <span class="arrow">→</span>
    <span class="arrow-label">writes</span>
    <div class="component" data-type="database">payments_db</div>
  </div>
  <div class="row">
    <div class="component" data-type="service">PaymentService</div>
    <span class="arrow">→</span>
    <span class="arrow-label">enqueue</span>
    <div class="component" data-type="queue">payment_events</div>
  </div>
</div>
```

`data-type` options: `service`, `external`, `database`, `queue`, `cache`.

### Failure modes table (with critical gap highlight)

```html
<table class="failure-modes">
  <thead>
    <tr>
      <th>#</th>
      <th>Scenario</th>
      <th>Test?</th>
      <th>Handling?</th>
      <th>UX</th>
      <th>Severity</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>Network timeout to upstream</td>
      <td>✓</td>
      <td>✓ retry</td>
      <td>Loud retry message</td>
      <td><span class="badge severity-major">Major</span></td>
    </tr>
    <tr class="critical-gap">
      <td>2</td>
      <td>Race condition di concurrent write</td>
      <td>✗</td>
      <td>✗</td>
      <td>Silent corruption</td>
      <td><span class="badge severity-block">Critical</span></td>
    </tr>
  </tbody>
</table>
```

`<tr class="critical-gap">` triggers red-tinted bg + red left border = visual warning.

### Forcing question callout

```html
<div class="forcing-question">
  <strong>Q1 (Risk tier):</strong> Lo akui ini T0 atau T1?
  <ul>
    <li>a) T0 (irreversible op, sensitive data)</li>
    <li>b) T1 (schema migration, breaking API)</li>
    <li>c) T2 (single-module additive)</li>
  </ul>
</div>
```

### Anti-pattern bullet list

```html
<ul>
  <li><div class="anti-pattern">Skip risk tier — bikin downstream gak punya gate yang bener.</div></li>
  <li><div class="anti-pattern">Jumping ke implementation tanpa state diagnosis.</div></li>
</ul>
```

### Tables (general)

Use plain `<table>` — CSS handles zebra rows + header bg automatically.

```html
<table>
  <thead><tr><th>Col 1</th><th>Col 2</th></tr></thead>
  <tbody>
    <tr><td>...</td><td>...</td></tr>
  </tbody>
</table>
```

### Code blocks (inline + block)

Inline:
```html
<code>git rev-parse</code>
```

Block:
```html
<pre><code>git -C repo log --oneline -3
git -C repo status</code></pre>
```

---

## TOC Auto-Generation

Build dari `<h2>` (top-level) dan `<h3>` (nested). Setiap heading wajib punya `id` (slug dari heading text).

Example:

```html
<nav class="toc" aria-label="Table of contents">
  <h2>Contents</h2>
  <ol>
    <li><a href="#tldr">TL;DR</a></li>
    <li><a href="#scope">Step-0 Scope Findings</a></li>
    <li>
      <a href="#architecture">Architecture</a>
      <ol>
        <li><a href="#component-boundaries">Component boundaries</a></li>
        <li><a href="#data-flow">Data flow</a></li>
        <li><a href="#trust-boundaries">Trust boundaries</a></li>
      </ol>
    </li>
    <li><a href="#invariants">Invariants</a></li>
    <li><a href="#failure-modes">Failure Modes</a></li>
    <li><a href="#test-strategy">Test Strategy</a></li>
    <li><a href="#handoff">Handoff</a></li>
  </ol>
</nav>
```

Heading dengan matching id:

```html
<h2 id="tldr">TL;DR</h2>
```

Slug rule: lowercase, replace space dengan `-`, strip non-alphanumeric kecuali `-`.

---

## Breadcrumb

Inline di `<header>`. Format:

```
em-thinking › <skill-name> › <artifact-short-title>
```

Contoh untuk em-plan:
```html
<nav class="breadcrumb" aria-label="Breadcrumb">
  <a href="#">em-thinking</a>
  <span class="separator">›</span>
  <a href="#">/em-plan</a>
  <span class="separator">›</span>
  <span aria-current="page">Payment Provider Integration</span>
</nav>
```

Contoh untuk em-review Mode A:
```html
<nav class="breadcrumb" aria-label="Breadcrumb">
  <a href="#">em-thinking</a>
  <span class="separator">›</span>
  <a href="#">/em-review</a>
  <span class="separator">›</span>
  <a href="#">Mode A — Code Review</a>
  <span class="separator">›</span>
  <span aria-current="page">PR #1234</span>
</nav>
```

---

## Common Mistakes

- ❌ **Skip HTML output** — user explicitly review via HTML, mandatory.
- ❌ **External CDN** (e.g. `<link href="https://cdn...">`) — breaks file:// loading + offline.
- ❌ **Mismatch .md ↔ .html content** — must be 1:1. HTML is rendering, not different content.
- ❌ **JavaScript dependencies** — tidak boleh, HTML harus pure static.
- ❌ **Hardcoded inline styles per element** — semua styling lewat class. CSS sudah inline di `<style>`.
- ❌ **Missing TOC** — required even untuk artifact pendek (3-5 sections).
- ❌ **Critical gap row tanpa class="critical-gap"** — kehilangan visual signal.
- ❌ **Risk tier mention tanpa `<span class="badge risk-tX">`** — lose color coding.

---

## File Output Convention

Same directory:
```
<working-dir>/
├── edd.md         ← source markdown
└── edd.html       ← rendered review version
```

Filenames must match (just different extensions). Never produce only one — always pair.

For PR reviews:
```
<working-dir>/
├── pr-review-<sha>.md
└── pr-review-<sha>.html
```

`<sha>` is short SHA (7 chars) of the PR head commit.
